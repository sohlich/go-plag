[log]
path=log/plag.log

[mongo]
port = 27017
host= localhost
assignments = assignments
submissions = submissions
results = results
database = plag

[server]
port = 8080
host = localhost

[apac]
url=http://localhost:9080/apac/sync/plagiarism

[elastic]
url=http://localhost:9200
password=elasticadmin
username=elasticadmin